<nav class="navigation navbar navbar-expand-md  navbar-dark bg-dark">
    <button class="navbar-toggler" type="button"
            data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
            aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <h1 class="header-logo navbar-brand">
            <a class="" href="#" title="Accueil">
                <img src="{{ asset('img/logo_ENI_global.svg') }}" alt="logo ENI">
            </a>
            <a href="{{ path('main_home') }}">On va sortir</a>
        </h1>

        <ul class="navbar-nav " >
            {#        <li><a href="{{ path('sorties_findSelect') }}" title="Liste des sorties">Sorties</a></li>#}

            {%  if app.user %}
                <li class="nav-item"><a href="{{ path('user_show', {'id': app.user.id}) }}" title="Profil">Profil de {{ app.user.userIdentifier }}</a></li>
                <li class="nav-item"><a href="{{ path('app_logout') }}" title="Se déconnecter">Se déconnecter</a> </li>
                <li class="nav-item"><a href="{{ path('sorties_create') }}" title="Proposer une sortie">Ajout sortie</a></li>
                <li class="nav-item"><a href="{{ path('lieu_index') }}">Gerer lieux</a></li>

                {% if is_granted("ROLE_ADMIN") %}
                    <li class="nav-item"><a href="{{ path('admin_dashboard') }}" title="Admin Dashboard">Admin</a> </li>
                {% endif %}

            {% else %}
                <li class="nav-item"><a href="{{ path('app_login') }}" title="Se connecter">Se connecter</a> </li>
                <li class="nav-item"><a href="{{ path('app_register') }}" title="S'inscrire'">S'inscrire</a> </li>
            {% endif %}
        </ul>
    </div>

</nav>