{% extends 'base.html.twig' %}

{% block title %}Ville{% endblock %}

{% block body %}
    <h1>Ville - détails</h1>

    <table class="table table-dark">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ville.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ ville.nom }}</td>
            </tr>
            <tr>
                <th>CodePostal</th>
                <td>{{ ville.codePostal }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-success" href="{{ path('crud_ville_index') }}">Retour</a>

    <a class="btn btn-primary" href="{{ path('crud_ville_edit', {'id': ville.id}) }}">Modifier</a>

    {{ include('admin/crud_ville/_delete_form.html.twig') }}
{% endblock %}
