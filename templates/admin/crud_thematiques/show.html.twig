{% extends 'base.html.twig' %}

{% block title %}Thematiques Détails{% endblock %}

{% block body %}
    <h1>Thématique - détails</h1>

    <table class="table table-dark">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ thematique.id }}</td>
            </tr>
            <tr>
                <th>Theme</th>
                <td>{{ thematique.Theme }}</td>
            </tr>
        </tbody>
    </table>
    <p>
    <a class="btn btn-success" href="{{ path('crud_thematiques_index') }}">Retour</a>
    </p><p>
    <a class="btn btn-primary" href="{{ path('crud_thematiques_edit', {'id': thematique.id}) }}">Modifier</a>
    </p>
    {{ include('admin/crud_thematiques/_delete_form.html.twig') }}
{% endblock %}
