{% extends 'base.html.twig' %}

{% block title %}Thematiques{% endblock %}

{% block body %}
    <h1>Thématiques</h1>

    <table class="table table-dark">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ thematique.id }}</td>
            </tr>
            <tr>
                <th>Theme</th>
                <td>{{ thematique.Theme }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('crud_thematiques_index') }}">Retour</a>

    <a href="{{ path('crud_thematiques_edit', {'id': thematique.id}) }}">Modifier</a>

    {{ include('admin/crud_thematiques/_delete_form.html.twig') }}
{% endblock %}
