{% extends 'base.html.twig' %}

{% block title %}Campus{% endblock %}

{% block body %}
    <h1>Campus</h1>

    <table class="table table-dark">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ campus.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ campus.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('crud_campus_index') }}">Retour</a>

    <a href="{{ path('crud_campus_edit', {'id': campus.id}) }}">Modifier</a>

    {{ include('admin/crud_campus/_delete_form.html.twig') }}
{% endblock %}
