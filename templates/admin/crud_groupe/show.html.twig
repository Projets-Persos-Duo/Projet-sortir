{% extends 'base.html.twig' %}

{% block title %}Groupe{% endblock %}

{% block body %}
    <h1>Groupe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ groupe.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('crud_groupe_index') }}">back to list</a>

    <a href="{{ path('crud_groupe_edit', {'id': groupe.id}) }}">edit</a>

    {{ include('admin/crud_groupe/_delete_form.html.twig') }}
{% endblock %}
