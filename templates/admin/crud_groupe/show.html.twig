{% extends 'base.html.twig' %}

{% block title %}Groupe détails{% endblock %}

{% block body %}
    <h1>Groupe - détails</h1>

    <table class="table table-dark">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ groupe.id }}</td>
            </tr>
        </tbody>
    </table>
    <p>
    <a class="btn btn-success" href="{{ path('crud_groupe_index') }}">Retour</a>
    </p><p>
    <a class="btn btn-primary" href="{{ path('crud_groupe_edit', {'id': groupe.id}) }}">Modifier</a>
    </p>
    {{ include('admin/crud_groupe/_delete_form.html.twig') }}
{% endblock %}
