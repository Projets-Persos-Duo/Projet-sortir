{% extends 'base.html.twig' %}

{% block title %}Détails utilisateur{% endblock %}

{% block body %}
    <h1>Utilisateur - détails</h1>

    <table class="table table-dark">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ user.userIdentifier }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
{#            <tr>#}
{#                <th>Password</th>#}
{#                <td>{{ user.password }}</td>#}
{#            </tr>#}
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>FamilyName</th>
                <td>{{ user.familyName }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ user.firstName }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ user.telephone }}</td>
            </tr>
            <tr>
                <th>IsAdmin</th>
                <td>{{ user.isAdmin ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ user.isActive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>
    <p>
    <a class="btn btn-success" href="{{ path('crud_user_index') }}">Retour</a>
    </p><p>
    <a class="btn btn-primary" href="{{ path('crud_user_edit', {'id': user.id}) }}">Modifier</a>
    </p>    <p>
    {{ include('admin/crud_user/_delete_form.html.twig') }}
</p>
        {{ include('admin/crud_user/_archiver_form.html.twig') }}

{% endblock %}
