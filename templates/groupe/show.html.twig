{% extends 'base.html.twig' %}
{% import 'macros.html.twig' as macros %}

{% block title %}Groupe{% endblock %}

{% block body %}
    <h1>Groupe</h1>

    <table class="table table-dark">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ groupe.id }}</td>
                <td>{{ groupe.proprietaire }}</td>
                <td>{{ macros.liste_utilisateurs(groupe.membres) }}</td>
            </tr>
        </tbody>
    </table>
<p>
    <a class="btn btn-success" href="{{ path('groupe_index') }}">Retour à la liste</a>
</p> <p>
    <a class="btn btn-primary" href="{{ path('groupe_edit', {'id': groupe.id}) }}">Modifier</a>
    </p>
    {{ include('groupe/_delete_form.html.twig') }}
{% endblock %}
