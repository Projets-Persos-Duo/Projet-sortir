{% extends 'base.html.twig' %}

{% block title %}Groupe{% endblock %}

{% block body %}
    <h1>Groupe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ groupe.id }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary" href="{{ path('groupe_index') }}">Retour à la liste"</a>

    <a class="btn btn-primary" href="{{ path('groupe_edit', {'id': groupe.id}) }}">Modifier</a>

    {{ include('groupe/_delete_form.html.twig') }}
{% endblock %}
