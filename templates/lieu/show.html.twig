{% extends 'base.html.twig' %}

{% block title %}Lieu détails{% endblock %}

{% block body %}
    <h1>Lieu - détails</h1>

    <table class="table table-dark">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lieu.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ lieu.nom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ lieu.adresse }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ lieu.latitude }}</td>
            </tr>
            <tr>
                <th>Longitude</th>
                <td>{{ lieu.longitude }}</td>
            </tr>
        </tbody>
    </table>
    <p>
    <a class="btn btn-success" href="{{ path('lieu_index') }}">Retour</a>
    </p>
    <p>
    <a class="btn btn-primary" href="{{ path('lieu_edit', {'id': lieu.id}) }}">Modifier</a>
  </p>
    {{ include('lieu/_delete_form.html.twig') }}
{% endblock %}
