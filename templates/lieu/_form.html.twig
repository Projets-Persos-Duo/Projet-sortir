{{ form_start(form) }}
    {{ form_widget(form) }}
    <p>
        <a href="{{ path('ville_new') }}" >Ajouter une ville</a>
    </p>

    <button class="btn btn-success">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
