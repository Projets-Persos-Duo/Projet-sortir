{{ form_start(form) }}
    {{ form_widget(form) }}
    <p>
        <a class="btn btn-warning" href="{{ path('ville_new') }}" >Ajouter une ville</a>
    </p>

    <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
